<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>villager-poi-halt</artifactId>
    <version>1.8.0</version>
    <packaging>jar</packaging>

    <name>VillagerPoiHalt</name>
    <description>
        Works around https://github.com/PaperMC/Folia/issues/292 by disabling villager AI
        (Brain.tick) so AcquirePoi / YieldJobSite / PoiCompetitorScan never call
        PoiManager.getOrLoad() and never sync-load POI data on a region tick thread.
    </description>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <repositories>
        <!-- Paper API (also what Folia servers expose to plugins) -->
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
        <!-- WorldGuard (optional soft-dependency, compile-time only) -->
        <repository>
            <id>enginehub</id>
            <url>https://maven.enginehub.org/repo/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>1.21.4-R0.1-SNAPSHOT</version>
            <scope>provided</scope>
        </dependency>
        <!--
            Optional: WorldGuard region scoping for "disabled-regions".
            scope=provided, never shaded; the hook class is only touched at runtime
            when the WorldGuard plugin is actually installed.
        -->
        <dependency>
            <groupId>com.sk89q.worldguard</groupId>
            <artifactId>worldguard-bukkit</artifactId>
            <version>7.0.10</version>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>VillagerPoiHalt-${project.version}</finalName>
        <resources>
            <!-- Filter plugin.yml so ${project.version} is substituted -->
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
            </plugin>
        </plugins>
    </build>
</project>
